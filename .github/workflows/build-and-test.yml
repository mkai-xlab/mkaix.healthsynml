name: ML CI

on:
  push:
    branches:
      - dev
      - main
    paths:
      - "app/**"
      - "tests/**"
      - "main.py"
      - "requirements.txt"
      - "Dockerfile"
      - "docker-compose.yml"
      - "requirements-ci.txt"
      - ".github/workflows/**"
  pull_request:
    branches:
      - dev
      - main
    paths:
      - "app/**"
      - "tests/**"
      - "main.py"
      - "requirements.txt"
      - "Dockerfile"
      - "docker-compose.yml"
      - "requirements-ci.txt"
      - ".github/workflows/**"

permissions:
  contents: read

jobs:
  build-and-test:
    if: >-
      github.event_name == 'push' ||
      github.base_ref == 'dev' ||
      (github.base_ref == 'main' && github.head_ref == 'dev')
    name: Test and build for ${{ github.ref_name || github.base_ref }}
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: requirements-ci.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-ci.txt

      - name: Run tests with pytest
        run: python -m pytest -q --disable-warnings --maxfail=1
